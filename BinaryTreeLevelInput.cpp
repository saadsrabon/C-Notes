#include<bits/stdc++.h>
using namespace std;

class Node {
    public:
    int val;
    Node* left;
    Node* right;
    Node(int val)
    {
        this->val = val;
        this->left =NULL;
        this->right =NULL;
    };
};

// Take input for Level Order

Node input_tree ()
{
    // First we need to create the root first
}
int main() 
{
    
    return 0;
}